# ─────────────────────────────────────────────────────────────────────────────
# .env.example — Environment Variable Template
#
# HOW TO USE:
#   1. Copy this file:  cp .env.example .env
#   2. Fill in every value marked with <angle brackets>
#   3. NEVER commit .env — it's in .gitignore
#
# FOR RENDER:
#   Set these in Render Dashboard → Your Service → Environment
#   OR define them in render.yaml under envVars (non-secret values only)
#   Secret values (SECRET_KEY, DB password) must be set in the dashboard.
# ─────────────────────────────────────────────────────────────────────────────

# ── Django Core ───────────────────────────────────────────────────────────────
# Generate a strong key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=<your-strong-secret-key-here>

# Set to False in production — True only for local development
DEBUG=False

# Comma-separated list of allowed hostnames
# Add your Render URL and any custom domain
ALLOWED_HOSTS=suits-webapp-backend.onrender.com,localhost,127.0.0.1

# ── Database ──────────────────────────────────────────────────────────────────
# Render automatically injects DATABASE_URL when you link a Postgres database.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<dbname>

# ── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of frontend origins allowed to call this API.
# No trailing slash. Include your Netlify/Vercel/Render frontend URL.
CORS_ALLOWED_ORIGINS=https://new-suits-systems.onrender.com,http://localhost:3000

# ── Cloudflare R2 Storage (optional — leave blank to disable) ─────────────────
CLOUDFLARE_R2_KEY_ID=
CLOUDFLARE_R2_SECRET_KEY=
CLOUDFLARE_R2_BUCKET=
CLOUDFLARE_R2_ACCOUNT_ID=

# ── Gunicorn Tuning (optional — defaults shown) ───────────────────────────────
# 2 workers fits Render free tier (512 MB RAM). Increase for paid plans.
GUNICORN_WORKERS=2
# Seconds before a worker is killed if it doesn't respond. 120 covers cold starts.
GUNICORN_TIMEOUT=120